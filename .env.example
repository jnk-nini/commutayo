VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-or-publishable-key

# Feature flag: routes over the live Supabase network instead of the hardcoded pilot corridor.
# Leave "false" until the database is populated -- see src/utils/dynamicRoutingEngine.ts.
VITE_USE_DYNAMIC_NETWORK=false
